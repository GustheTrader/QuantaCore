# ============================================================
# OpenNoesis Requirements
# OpenFang × AgentOS Cognitive Kernel 
# ============================================================

# ─── Redis + Memory Layer ─────────────────────────────────────
redis[hiredis]>=5.0.0
langgraph>=0.2.0
langgraph-checkpoint-redis>=0.1.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0

# ─── Graph Knowledge (Memgraph / Neo4j via Bolt) ──────────────
neo4j>=5.18.0             # Works with both Neo4j and Memgraph (Bolt compatible)
gqlalchemy>=1.4.0         # Memgraph Python OGM (optional, for schema-aware ops)

# ─── Vision / Graph Video Coding ──────────────────────────────
opencv-python>=4.9.0      # Frame extraction
Pillow>=10.3.0            # Image processing

# ─── LLM Providers (LiteLLM unified interface) ────────────────
litellm>=1.35.0
openai>=1.25.0            # GPT-4o, GPT-4o Vision
anthropic>=0.25.0         # Claude 3.5 Sonnet
google-generativeai>=0.5.0  # Gemini Vision

# ─── API / Bridge Server ──────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ─── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0
tenacity>=8.3.0
rich>=13.7.0
loguru>=0.7.2
PyYAML>=6.0

# ─── Development / Testing ────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
black>=24.4.0
